[mgr]
show_hidden = true
show_symlink = true

[preview]
image_filter = "lanczos3"
image_quality = 90

[opener]
play = [
	{ run = "xdg-open %s", orphan = true, for = "unix" },
]
edit = [
	{ run = "nano %s", block = true, for = "unix" },
]
open = [
	{ run = "xdg-open %s", desc = "Open" },
]

